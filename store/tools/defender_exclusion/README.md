# defender_exclusion

Manage Windows Defender (Antimalware) scan exclusions — add, remove, or list excluded paths, processes and extensions. Requires admin. Uses PowerShell MpPreference cmdlets.

## Structure

- `schema.json`: Interface
- `tool.py`: CLI
- worker.py: Background
