{
  "id": "tool:sync",
  "name": "navig_sync",
  "description": "Execute NAVIG sync operations.",
  "version": "1.0",
  "parameters": {
    "type": "object",
    "properties": {
      "args": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Arguments for the command"
      }
    },
    "required": [
      "args"
    ]
  },
  "execution": {
    "type": "cli",
    "target": "navig sync"
  },
  "requires_approval": false
}
