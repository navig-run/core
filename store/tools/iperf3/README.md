# iperf3

Network bandwidth testing — run as client against a remote server, or start a local server. Uses the iperf3 binary from C:/USB/network/iperf3/ on Windows; system iperf3 on Linux/Mac.

## Structure

- `schema.json`: Interface
- `tool.py`: CLI
- worker.py: Background
