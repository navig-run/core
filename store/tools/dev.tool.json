{
  "id": "tool:dev",
  "name": "navig_dev",
  "description": "Execute NAVIG dev operations.",
  "version": "1.0",
  "parameters": {
    "type": "object",
    "properties": {
      "args": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Arguments for the command"
      }
    },
    "required": [
      "args"
    ]
  },
  "execution": {
    "type": "cli",
    "target": "navig dev"
  },
  "requires_approval": false
}