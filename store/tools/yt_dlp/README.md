# yt_dlp

Download videos, audio, or metadata from YouTube and 1000+ sites using yt-dlp. Cross-platform — requires yt-dlp installed (pip install yt-dlp).

## Structure

- `schema.json`: Interface
- `tool.py`: CLI
- worker.py: Background
