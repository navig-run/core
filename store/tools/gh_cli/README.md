# gh_cli

GitHub CLI wrapper — create PRs, list issues, view repo status, manage releases. Cross-platform wrapper around the 'gh' binary. Requires gh installed and authenticated.

## Structure

- `schema.json`: Interface
- `tool.py`: CLI
- worker.py: Background
