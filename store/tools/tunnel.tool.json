{
  "id": "tool:tunnel",
  "name": "navig_tunnel",
  "description": "Execute NAVIG tunnel operations.",
  "version": "1.0",
  "parameters": {
    "type": "object",
    "properties": {
      "args": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Arguments for the command"
      }
    },
    "required": [
      "args"
    ]
  },
  "execution": {
    "type": "cli",
    "target": "navig tunnel"
  },
  "requires_approval": false
}