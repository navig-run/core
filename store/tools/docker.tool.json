{
  "id": "tool:docker",
  "name": "navig_docker",
  "description": "Execute NAVIG docker operations.",
  "version": "1.0",
  "parameters": {
    "type": "object",
    "properties": {
      "args": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Arguments for the command"
      }
    },
    "required": [
      "args"
    ]
  },
  "execution": {
    "type": "cli",
    "target": "navig docker"
  },
  "requires_approval": true
}
