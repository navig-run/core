# screenshot

Take a screenshot of the full screen or a specific monitor. Cross-platform: uses Win32 API on Windows, screencapture on macOS, scrot/gnome-screenshot on Linux.

## Structure

- `schema.json`: Interface
- `tool.py`: CLI
- worker.py: Background
