# NAVIG - Cross-Platform SSH Tunnel & Remote Server Management CLI
# Python 3.10+ required

# CLI Framework
typer[all]>=0.14.0

# Terminal UI and Formatting
rich>=13.7.0
# questionary>=2.0.0  # OPTIONAL: Better keyboard navigation (may cause issues on some Windows systems)
# If experiencing freezes or resource errors, remove questionary and menu will use number-based selection

# Configuration
pyyaml>=6.0

# HTTP Requests (for OpenRouter API and gateway client)
requests>=2.31.0

# Async HTTP Server (for gateway)
aiohttp>=3.9.0

# WebSocket (for MCP WebSocket transport)
websockets>=15.0

# SSH and networking (using native ssh command, but paramiko as fallback)
paramiko>=3.4.0

# Cross-platform ANSI colors (Rich handles most of this, but backup)
colorama>=0.4.6

# Vault encryption
cryptography>=42.0.0

# Async HTTP (for validators and API calls)
httpx>=0.27.0

# JSON handling (built-in, but explicit for clarity)
# json - stdlib

# File system operations
# pathlib - stdlib
# os - stdlib
# subprocess - stdlib

# Date/time handling
# datetime - stdlib

# Process management
# signal - stdlib
psutil>=5.9.0  # For cross-platform process management

# Clipboard operations (for assistant context export)
pyperclip>=1.8.2
Jinja2>=3.1.0

# Network speed testing (for navig net speedtest)
speedtest-cli>=2.1.3

# Optional: Browser Automation (for navig browser commands)
# playwright>=1.40.0  # Install separately: pip install playwright && playwright install chromium

# Optional: Desktop Automation (for navig desktop commands)
# pyautogui>=0.9.54  # Requires display, may not work on headless servers
# Pillow>=10.0.0     # Image processing for desktop automation

# Windows Desktop Agent dependencies (navig-desktop-agent, Windows only)
# Install on Windows: pip install uiautomation comtypes
uiautomation>=2.0.18 ; sys_platform == "win32"
comtypes>=1.2.0      ; sys_platform == "win32"

# Optional: File System Watching (for reactive automation)
# watchdog>=3.0.0    # Cross-platform file system events

# Optional: Memory/RAG Features (for navig memory commands)
# These enable semantic search and embeddings for conversation context
# sentence-transformers>=2.2.0  # Local vector embeddings (~500MB model download)
# chromadb>=0.4.0               # Vector database for similarity search
# tiktoken>=0.5.0               # Token counting for context management
# openai>=1.0.0                 # Optional: Remote embeddings via OpenAI API

# Optional: Local vector search (replaces pgvector for single-user mode)
# sqlite-vec>=0.1.1             # ANN search inside SQLite (~2MB, cross-platform)
