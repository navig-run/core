# NAVIG Hello Plugin - Requirements
#
# This file lists external dependencies required by the plugin.
# These are optional - NAVIG will work without them but the plugin won't load.
#
# For this example plugin, there are no external dependencies.
# All imports (typer, rich) are already part of NAVIG's core dependencies.
#
# Example format for plugins with dependencies:
# openai>=1.0.0
# chromadb>=0.4.0
# pandas>=2.0.0
