{
  "id": "tool:cloud",
  "name": "navig_cloud",
  "description": "Execute NAVIG cloud operations.",
  "version": "1.0",
  "parameters": {
    "type": "object",
    "properties": {
      "args": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Arguments for the command"
      }
    },
    "required": [
      "args"
    ]
  },
  "execution": {
    "type": "cli",
    "target": "navig cloud"
  },
  "requires_approval": false
}
