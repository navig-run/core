# rclone

Cloud storage sync and management via rclone. Uses bundled rclone binary from C:/USB/network/rclone-browser/rclone/ on Windows; system rclone on Linux/Mac.

## Structure

- `schema.json`: Interface
- `tool.py`: CLI
- worker.py: Background
