"""NAVIG LLM subsystem — generation + provider/model routing.

- `generate`      : the llm_generate entrypoint
- `router`        : the high-level LLM router facade
- `types`         : LLMResult / ModelSelection
- `routing/`      : the routing engine (capabilities · detect · router · trace)
"""
