"""NAVIG Media — the context-aware, iterative, versioned generation workflow.

Sits on top of the stateless generators in :mod:`navig.tools` and adds:
  * :mod:`navig.media.refs_library`  — non-destructive, versioned on-disk library
  * :mod:`navig.media.context_builder` — project palette + style prompt enrichment
  * :mod:`navig.media.generation_service` — generate / re-roll / keep / reject
"""
