# calendar

Calendar operations and event management.

Common commands:
- `navig calendar list` — list upcoming events
- `navig calendar auth` — authenticate with calendar provider
- `navig calendar add` — add new calendar event
- `navig calendar sync` — sync calendar data

Examples:
- `navig calendar list --limit 10`
- `navig calendar auth`
