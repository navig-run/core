# search

Search the web for information.

Common commands:
- `navig search <query>` — search the web
- `navig search <query> --limit 5` — limit results
- `navig search <query> --provider brave` — use specific provider

Examples:
- `navig search "nginx proxy_pass configuration"`
- `navig search "docker compose volumes" --limit 3`
