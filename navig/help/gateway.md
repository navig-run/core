# gateway

Autonomous agent gateway server (24/7 control plane).

Common commands:
- `navig gateway start` — start the gateway server
- `navig gateway stop` — stop the gateway server
- `navig gateway status` — show gateway status
- `navig gateway session` — manage sessions

Examples:
- `navig gateway start --port 8080`
- `navig gateway status --json`
