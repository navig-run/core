# scaffold

Generate project structures from templates.

Common commands:
- `navig scaffold list` — list available templates
- `navig scaffold apply <template>` — generate files from template
- `navig scaffold validate <template>` — check template syntax

Examples:
- `navig scaffold list`
- `navig scaffold apply nextjs-app --name myproject`
