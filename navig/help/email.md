# email

Email operations and inbox management.

Common commands:
- `navig email list` — list unread emails
- `navig email setup` — configure email provider
- `navig email search <query>` — search emails
- `navig email send` — send an email

Examples:
- `navig email list --limit 20`
- `navig email search "invoice"`
